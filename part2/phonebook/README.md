## build instructions
Use `VITE_API_DOMAIN=MY_DOMAIN npm run build`, where `MY_DOMAIN` does not have
a trailing forward slash.

To set the base path for the front end itself, modify the variable `base` inside
`defineConfig()` in the `vite.config.js` file.

